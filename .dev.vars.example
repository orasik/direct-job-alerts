# Copy to `.dev.vars` for local development (`npm run dev`).
# In production these are set as secrets via `wrangler secret put <NAME>`.
SERPER_API_KEY=your_serper_api_key_here
WEBHOOK_URL=https://example.com/your-webhook
